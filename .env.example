# GitHub repo config for DevLog
VITE_GITHUB_OWNER=your-github-username
VITE_GITHUB_REPO=your-private-repo-name
VITE_GITHUB_FILE_PATH=data/tasks.json

# Google Sheet upload uses Apps Script Web App URL
# Configure in the app: Settings → Google Sheet (no env var required)
